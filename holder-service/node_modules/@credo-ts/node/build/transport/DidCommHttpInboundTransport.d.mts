import { AgentContext } from "@credo-ts/core";
import * as http0 from "http";
import { Server } from "http";
import { DidCommInboundTransport } from "@credo-ts/didcomm";
import { Express } from "express";

//#region src/transport/DidCommHttpInboundTransport.d.ts
declare class DidCommHttpInboundTransport implements DidCommInboundTransport {
  readonly app: Express;
  private port;
  private path;
  private _server?;
  private processedMessageListenerTimeoutMs;
  get server(): Server<typeof http0.IncomingMessage, typeof http0.ServerResponse> | undefined;
  constructor({
    app,
    path,
    port,
    processedMessageListenerTimeoutMs
  }: {
    app?: Express;
    path?: string;
    port: number;
    processedMessageListenerTimeoutMs?: number;
  });
  start(agentContext: AgentContext): Promise<void>;
  stop(): Promise<void>;
}
//#endregion
export { DidCommHttpInboundTransport };
//# sourceMappingURL=DidCommHttpInboundTransport.d.mts.map