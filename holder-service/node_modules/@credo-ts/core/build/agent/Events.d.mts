import { Observable } from "rxjs";

//#region src/agent/Events.d.ts
declare function filterContextCorrelationId(contextCorrelationId: string): <T extends BaseEvent>(source: Observable<T>) => Observable<T>;
interface EventMetadata {
  contextCorrelationId: string;
}
interface BaseEvent {
  type: string;
  payload: Record<string, unknown>;
  metadata: EventMetadata;
}
//#endregion
export { BaseEvent, EventMetadata, filterContextCorrelationId };
//# sourceMappingURL=Events.d.mts.map