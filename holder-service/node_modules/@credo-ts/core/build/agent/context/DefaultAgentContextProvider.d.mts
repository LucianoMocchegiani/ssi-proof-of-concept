import { AgentContext } from "./AgentContext.mjs";
import { AgentContextProvider } from "./AgentContextProvider.mjs";

//#region src/agent/context/DefaultAgentContextProvider.d.ts
/**
 * Default implementation of AgentContextProvider.
 *
 * Holds a single `AgentContext` instance that will be used for all messages, i.e. a
 * a single tenant agent.
 */
declare class DefaultAgentContextProvider implements AgentContextProvider {
  private agentContext;
  constructor(agentContext: AgentContext);
  getAgentContextForContextCorrelationId(contextCorrelationId: string): Promise<AgentContext>;
  getContextForInboundMessage(_: unknown, options?: {
    contextCorrelationId?: string;
  }): Promise<AgentContext>;
  endSessionForAgentContext(agentContext: AgentContext): Promise<void>;
  deleteAgentContext(agentContext: AgentContext): Promise<void>;
}
//#endregion
export { DefaultAgentContextProvider };
//# sourceMappingURL=DefaultAgentContextProvider.d.mts.map