

import { filter } from "rxjs";

//#region src/agent/Events.ts
function filterContextCorrelationId(contextCorrelationId) {
	return (source) => {
		return source.pipe(filter((event) => event.metadata.contextCorrelationId === contextCorrelationId));
	};
}

//#endregion
export { filterContextCorrelationId };
//# sourceMappingURL=Events.mjs.map