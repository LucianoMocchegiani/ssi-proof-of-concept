

import { Sha1 } from "../../hashes/Sha1.mjs";
import "../../hashes/index.mjs";
import * as core from "webcrypto-core";

//#region src/crypto/webcrypto/providers/CredoSha1Provider.ts
var CredoSha1Provider = class extends core.ProviderCrypto {
	constructor(..._args) {
		super(..._args);
		this.name = "SHA-1";
		this.usages = [];
	}
	async onDigest(algorithm, data) {
		switch (algorithm.name.toUpperCase()) {
			case "SHA-1": return new Sha1().hash(new Uint8Array(data)).buffer;
			default: throw new Error(`Hashing algorithm: ${JSON.stringify(algorithm)} is not supported`);
		}
	}
};

//#endregion
export { CredoSha1Provider };
//# sourceMappingURL=CredoSha1Provider.mjs.map