

import { Sha256 } from "../../hashes/Sha256.mjs";
import "../../hashes/index.mjs";
import * as core from "webcrypto-core";

//#region src/crypto/webcrypto/providers/CredoSha256Provider.ts
var CredoSha256Provider = class extends core.ProviderCrypto {
	constructor(..._args) {
		super(..._args);
		this.name = "SHA-256";
		this.usages = [];
	}
	async onDigest(algorithm, data) {
		switch (algorithm.name.toUpperCase()) {
			case "SHA-256": return new Sha256().hash(new Uint8Array(data)).buffer;
			default: throw new Error(`Hashing algorithm: ${JSON.stringify(algorithm)} is not supported`);
		}
	}
};

//#endregion
export { CredoSha256Provider };
//# sourceMappingURL=CredoSha256Provider.mjs.map