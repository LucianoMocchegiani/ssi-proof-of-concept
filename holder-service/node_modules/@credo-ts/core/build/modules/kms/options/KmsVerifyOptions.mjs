

import { zKnownJwaSignatureAlgorithm } from "../jwk/jwa.mjs";
import { zAnyUint8Array } from "../../../utils/zod.mjs";
import { zKmsJwkPublicAsymmetric } from "../jwk/knownJwk.mjs";
import { zKmsKeyId } from "./common.mjs";
import { z } from "zod";

//#region src/modules/kms/options/KmsVerifyOptions.ts
const zKmsVerifyOptions = z.object({
	key: z.union([z.object({
		keyId: zKmsKeyId,
		publicJwk: z.never().optional()
	}), z.object({
		publicJwk: zKmsJwkPublicAsymmetric,
		keyId: z.never().optional()
	})]),
	algorithm: zKnownJwaSignatureAlgorithm.describe("The JWA signature algorithm to use for verification"),
	data: zAnyUint8Array.describe("The data to verify"),
	signature: zAnyUint8Array.describe("The signature on the data to verify")
});

//#endregion
export { zKmsVerifyOptions };
//# sourceMappingURL=KmsVerifyOptions.mjs.map