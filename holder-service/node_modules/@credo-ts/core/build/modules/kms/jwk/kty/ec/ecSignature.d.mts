import { AnyUint8Array, Uint8ArrayBuffer } from "../../../../../types.mjs";
import { KmsJwkPublicEc } from "./ecJwk.mjs";

//#region src/modules/kms/jwk/kty/ec/ecSignature.d.ts
/**
 * Converts a RAW EC signature to DER format
 *
 * @param rawSignature - Raw signature as r || s concatenated values
 * @param crv - The EC crv of the key used for the signature
 * @returns DER encoded signature
 */
declare function rawEcSignatureToDer(rawSignature: AnyUint8Array, crv: KmsJwkPublicEc['crv']): Uint8ArrayBuffer;
/**
 * Converts a DER encoded EC signature to RAW format
 *
 * @param derSignature - DER encoded signature
 * @param crv - The EC crv of the key used for the signature
 * @returns Raw signature as r || s concatenated values
 */
declare function derEcSignatureToRaw(derSignature: AnyUint8Array, crv: KmsJwkPublicEc['crv']): Uint8ArrayBuffer;
//#endregion
export { derEcSignatureToRaw, rawEcSignatureToDer };
//# sourceMappingURL=ecSignature.d.mts.map