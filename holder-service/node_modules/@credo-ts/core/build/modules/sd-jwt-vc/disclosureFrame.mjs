

import { isObject } from "class-validator";

//#region src/modules/sd-jwt-vc/disclosureFrame.ts
function buildDisclosureFrameForPayload(input) {
	return Object.fromEntries(Object.entries(input).map(([key, value]) => {
		if (Array.isArray(value)) return [key, true];
		if (isObject(value)) {
			if (Object.keys.length === 0) return [key, false];
			return [key, buildDisclosureFrameForPayload(value)];
		}
		return [key, true];
	}));
}

//#endregion
export { buildDisclosureFrameForPayload };
//# sourceMappingURL=disclosureFrame.mjs.map