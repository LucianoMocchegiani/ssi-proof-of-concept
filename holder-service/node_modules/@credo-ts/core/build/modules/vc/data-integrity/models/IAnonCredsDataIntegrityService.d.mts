import { W3cCredentialRecord } from "../../repository/W3cCredentialRecord.mjs";
import "../../repository/index.mjs";
import { W3cPresentation } from "../../models/presentation/W3cPresentation.mjs";
import "../../models/index.mjs";
import { DifPresentationExchangeDefinition, DifPresentationExchangeSubmission } from "../../../dif-presentation-exchange/models/index.mjs";
import "../../../dif-presentation-exchange/index.mjs";
import { W3cJsonLdVerifiablePresentation } from "./W3cJsonLdVerifiablePresentation.mjs";
import { AgentContext } from "../../../../agent/context/AgentContext.mjs";
import "../../../../agent/index.mjs";

//#region src/modules/vc/data-integrity/models/IAnonCredsDataIntegrityService.d.ts
declare const ANONCREDS_DATA_INTEGRITY_CRYPTOSUITE: "anoncreds-2023";
interface AnoncredsDataIntegrityCreatePresentation {
  selectedCredentialRecords: W3cCredentialRecord[];
  presentationDefinition: DifPresentationExchangeDefinition;
  presentationSubmission: DifPresentationExchangeSubmission;
  challenge: string;
}
interface AnoncredsDataIntegrityVerifyPresentation {
  presentation: W3cJsonLdVerifiablePresentation;
  presentationDefinition: DifPresentationExchangeDefinition;
  presentationSubmission: DifPresentationExchangeSubmission;
  challenge: string;
}
declare const AnonCredsDataIntegrityServiceSymbol: unique symbol;
/**
 * We keep this standalone and don't integrity it
 * with for example the SignatureSuiteRegistry due
 * to it's unique properties, in order to not pollute,
 * the existing api's.
 */
interface IAnonCredsDataIntegrityService {
  createPresentation(agentContext: AgentContext, options: AnoncredsDataIntegrityCreatePresentation): Promise<W3cPresentation>;
  verifyPresentation(agentContext: AgentContext, options: AnoncredsDataIntegrityVerifyPresentation): Promise<boolean>;
}
//#endregion
export { ANONCREDS_DATA_INTEGRITY_CRYPTOSUITE, AnonCredsDataIntegrityServiceSymbol, AnoncredsDataIntegrityCreatePresentation, AnoncredsDataIntegrityVerifyPresentation, IAnonCredsDataIntegrityService };
//# sourceMappingURL=IAnonCredsDataIntegrityService.d.mts.map