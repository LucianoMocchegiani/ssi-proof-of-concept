

//#region src/modules/vc/data-integrity/models/LdKeyPair.ts
var LdKeyPair = class {
	constructor(options) {
		this.id = options.id;
		this.controller = options.controller;
	}
	static async generate() {
		throw new Error("Not implemented");
	}
	static async from(_verificationMethod) {
		throw new Error("Abstract method from() must be implemented in subclass.");
	}
	export(publicKey = false, privateKey = false) {
		if (!publicKey && !privateKey) throw new Error("Export requires specifying either \"publicKey\" or \"privateKey\".");
		return {
			id: this.id,
			type: this.type,
			controller: this.controller
		};
	}
};

//#endregion
export { LdKeyPair };
//# sourceMappingURL=LdKeyPair.mjs.map