

import { PublicJwk } from "../../../kms/jwk/PublicJwk.mjs";
import "../../../kms/index.mjs";
import { parseDid } from "../../domain/parse.mjs";
import { getDidDocumentForPublicJwk } from "../../domain/keyDidDocument.mjs";

//#region src/modules/dids/methods/key/DidKey.ts
var DidKey = class DidKey {
	constructor(publicJwk) {
		this.publicJwk = publicJwk;
	}
	static fromDid(did) {
		const parsed = parseDid(did);
		return new DidKey(PublicJwk.fromFingerprint(parsed.id));
	}
	get did() {
		return `did:key:${this.publicJwk.fingerprint}`;
	}
	get didDocument() {
		return getDidDocumentForPublicJwk(this.did, this.publicJwk);
	}
};

//#endregion
export { DidKey };
//# sourceMappingURL=DidKey.mjs.map