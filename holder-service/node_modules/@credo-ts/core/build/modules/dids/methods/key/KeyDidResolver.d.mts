import { DidResolver } from "../../domain/DidResolver.mjs";
import { DidResolutionResult } from "../../types.mjs";
import { AgentContext } from "../../../../agent/context/AgentContext.mjs";
import "../../../../agent/index.mjs";

//#region src/modules/dids/methods/key/KeyDidResolver.d.ts
declare class KeyDidResolver implements DidResolver {
  readonly supportedMethods: string[];
  /**
   * No remote resolving done, did document is dynamically constructed. To not pollute the cache we don't allow caching
   */
  readonly allowsCaching = false;
  /**
   * Easier to calculate for resolving than serving the local did document. Record also doesn't
   * have a did document
   */
  readonly allowsLocalDidRecord = false;
  resolve(_agentContext: AgentContext, did: string): Promise<DidResolutionResult>;
}
//#endregion
export { KeyDidResolver };
//# sourceMappingURL=KeyDidResolver.d.mts.map