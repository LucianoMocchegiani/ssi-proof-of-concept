import { DidResolutionOptions, DidResolutionResult, ParsedDid } from "../types.mjs";
import { AgentContext } from "../../../agent/context/AgentContext.mjs";
import "../../../agent/index.mjs";

//#region src/modules/dids/domain/DidResolver.d.ts
interface DidResolver {
  readonly supportedMethods: string[];
  readonly allowsCaching: boolean;
  /**
   * Whether the resolver allows using a local created did document from
   * a did record to resolve the did document.
   *
   * @default false
   * @todo make required in 0.6.0
   */
  readonly allowsLocalDidRecord?: boolean;
  resolve(agentContext: AgentContext, did: string, parsed: ParsedDid, didResolutionOptions: DidResolutionOptions): Promise<DidResolutionResult>;
}
//#endregion
export { DidResolver };
//# sourceMappingURL=DidResolver.d.mts.map