

import { CredoError } from "../../error/CredoError.mjs";
import "../../error/index.mjs";

//#region src/modules/dcql/DcqlError.ts
var DcqlError = class extends CredoError {
	constructor(message, { cause, additionalMessages } = {}) {
		let fullMessage = message;
		if (additionalMessages?.length) fullMessage += `\n - ${additionalMessages.join("\n - ")}`;
		super(fullMessage, { cause });
		this.additionalMessages = additionalMessages;
	}
};

//#endregion
export { DcqlError };
//# sourceMappingURL=DcqlError.mjs.map