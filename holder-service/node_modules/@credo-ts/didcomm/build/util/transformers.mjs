import { DateTime } from "luxon";

//#region src/util/transformers.ts
function DateParser(value) {
	const parsedDate = new Date(value);
	if (parsedDate instanceof Date && !Number.isNaN(parsedDate.getTime())) return parsedDate;
	const luxonDate = DateTime.fromSQL(value);
	if (luxonDate.isValid) return new Date(luxonDate.toString());
	return /* @__PURE__ */ new Date();
}

//#endregion
export { DateParser };
//# sourceMappingURL=transformers.mjs.map