import { DidCommEncryptedMessage, DidCommPlaintextMessage } from "./types.mjs";
import { DidCommMessage } from "./DidCommMessage.mjs";
import { DidCommDocumentService } from "./services/DidCommDocumentService.mjs";
import { AgentContext, Kms, Logger } from "@credo-ts/core";

//#region src/DidCommEnvelopeService.d.ts
interface EnvelopeKeys {
  recipientKeys: Kms.PublicJwk<Kms.Ed25519PublicJwk>[];
  routingKeys: Kms.PublicJwk<Kms.Ed25519PublicJwk>[];
  senderKey: Kms.PublicJwk<Kms.Ed25519PublicJwk> | null;
}
declare class DidCommEnvelopeService {
  private logger;
  private didcommDocumentService;
  constructor(logger: Logger, didcommDocumentService: DidCommDocumentService);
  private encryptDidcommV1Message;
  private decryptDidcommV1Message;
  packMessage(agentContext: AgentContext, payload: DidCommMessage, keys: EnvelopeKeys): Promise<DidCommEncryptedMessage>;
  unpackMessage(agentContext: AgentContext, encryptedMessage: DidCommEncryptedMessage): Promise<DecryptedDidCommMessageContext>;
  private extractOurRecipientKeyWithKeyId;
}
interface DecryptedDidCommMessageContext {
  plaintextMessage: DidCommPlaintextMessage;
  senderKey?: Kms.PublicJwk<Kms.Ed25519PublicJwk>;
  recipientKey: Kms.PublicJwk<Kms.Ed25519PublicJwk>;
}
//#endregion
export { DidCommEnvelopeService, EnvelopeKeys };
//# sourceMappingURL=DidCommEnvelopeService.d.mts.map