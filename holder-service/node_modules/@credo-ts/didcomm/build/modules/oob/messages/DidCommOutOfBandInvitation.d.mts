import { DidCommPlaintextMessage } from "../../../types.mjs";
import { ParsedMessageType } from "../../../util/messageType.mjs";
import { DidCommMessage } from "../../../DidCommMessage.mjs";
import { DidCommAttachment } from "../../../decorators/attachment/DidCommAttachment.mjs";
import { OutOfBandDidCommService } from "../domain/OutOfBandDidCommService.mjs";
import "../../../index.mjs";

//#region src/modules/oob/messages/DidCommOutOfBandInvitation.d.ts
/**
 * The original invitation an out of band invitation was derived from.
 */
declare enum DidCommInvitationType {
  OutOfBand = "out-of-band/1.x",
  Connection = "connections/1.x",
  Connectionless = "connectionless"
}
interface DidCommOutOfBandInvitationOptions {
  id?: string;
  label?: string;
  goalCode?: string;
  goal?: string;
  accept?: string[];
  handshakeProtocols?: string[];
  services: Array<OutOfBandDidCommService | string>;
  imageUrl?: string;
  appendedAttachments?: DidCommAttachment[];
}
declare class DidCommOutOfBandInvitation extends DidCommMessage {
  constructor(options: DidCommOutOfBandInvitationOptions);
  /**
   * The original type of the invitation. This is not part of the RFC, but allows to identify
   * from what the oob invitation was originally created (e.g. legacy connectionless invitation).
   */
  invitationType?: DidCommInvitationType;
  addRequest(message: DidCommMessage): void;
  getRequests(): DidCommPlaintextMessage[] | undefined;
  toUrl({
    domain
  }: {
    domain: string;
  }): string;
  static fromUrl(invitationUrl: string): DidCommOutOfBandInvitation;
  static fromJson(json: Record<string, unknown>): DidCommOutOfBandInvitation;
  get invitationDids(): string[];
  getServices(): Array<OutOfBandDidCommService | string>;
  getDidServices(): Array<string>;
  getInlineServices(): Array<OutOfBandDidCommService>;
  readonly type: string;
  static readonly type: ParsedMessageType;
  readonly label?: string;
  readonly goalCode?: string;
  readonly goal?: string;
  readonly accept?: string[];
  handshakeProtocols?: string[];
  private requests?;
  private services;
  /**
   * Custom property. It is not part of the RFC.
   */
  readonly imageUrl?: string;
}
//#endregion
export { DidCommInvitationType, DidCommOutOfBandInvitation, DidCommOutOfBandInvitationOptions };
//# sourceMappingURL=DidCommOutOfBandInvitation.d.mts.map