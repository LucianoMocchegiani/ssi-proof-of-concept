import { DidCommCredentialProtocol } from "./protocol/DidCommCredentialProtocol.mjs";
import { DidCommCredentialsModuleConfig, DidCommCredentialsModuleConfigOptions } from "./DidCommCredentialsModuleConfig.mjs";
import { DidCommCredentialsApi } from "./DidCommCredentialsApi.mjs";
import { DidCommCredentialV2Protocol } from "./protocol/v2/DidCommCredentialV2Protocol.mjs";
import "./protocol/v2/index.mjs";
import { AgentContext, ApiModule, Constructor, DependencyManager, Optional } from "@credo-ts/core";

//#region src/modules/credentials/DidCommCredentialsModule.d.ts
/**
 * Default credentialProtocols that will be registered if the `credentialProtocols` property is not configured.
 */
type DefaultDidCommCredentialProtocols = [DidCommCredentialV2Protocol<[]>];
type DidCommCredentialsModuleOptions<CredentialProtocols extends DidCommCredentialProtocol[]> = Optional<DidCommCredentialsModuleConfigOptions<CredentialProtocols>, 'credentialProtocols'>;
declare class DidCommCredentialsModule<CredentialProtocols extends DidCommCredentialProtocol[] = DefaultDidCommCredentialProtocols> implements ApiModule {
  readonly config: DidCommCredentialsModuleConfig<CredentialProtocols>;
  readonly api: Constructor<DidCommCredentialsApi<CredentialProtocols>>;
  constructor(config?: DidCommCredentialsModuleOptions<CredentialProtocols>);
  /**
   * Registers the dependencies of the credentials module on the dependency manager.
   */
  register(dependencyManager: DependencyManager): void;
  initialize(agentContext: AgentContext): Promise<void>;
}
//#endregion
export { DefaultDidCommCredentialProtocols, DidCommCredentialsModule, DidCommCredentialsModuleOptions };
//# sourceMappingURL=DidCommCredentialsModule.d.mts.map