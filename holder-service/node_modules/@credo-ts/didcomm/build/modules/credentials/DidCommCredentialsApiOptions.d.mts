import { DidCommAutoAcceptCredential } from "./models/DidCommCredentialAutoAcceptType.mjs";
import { DidCommCredentialFormatPayload } from "./formats/DidCommCredentialFormat.mjs";
import "./formats/index.mjs";
import { CredentialFormatsFromProtocols, DeleteCredentialOptions, GetCredentialFormatDataReturn } from "./protocol/DidCommCredentialProtocolOptions.mjs";
import { DidCommCredentialProtocol } from "./protocol/DidCommCredentialProtocol.mjs";

//#region src/modules/credentials/DidCommCredentialsApiOptions.d.ts
type FindCredentialProposalMessageReturn<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> = ReturnType<CPs[number]['findProposalMessage']>;
type FindCredentialOfferMessageReturn<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> = ReturnType<CPs[number]['findOfferMessage']>;
type FindCredentialRequestMessageReturn<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> = ReturnType<CPs[number]['findRequestMessage']>;
type FindCredentialMessageReturn<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> = ReturnType<CPs[number]['findCredentialMessage']>;
/**
 * Get the supported protocol versions based on the provided credential protocols.
 */
type CredentialProtocolVersionType<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> = CPs[number]['version'];
interface BaseOptions {
  autoAcceptCredential?: DidCommAutoAcceptCredential;
  comment?: string;
  /**
   * Will be ignored for v1 protocol as it is not supported
   */
  goalCode?: string;
  /**
   * Will be ignored for v1 protocol as it is not supported
   */
  goal?: string;
}
/**
 * Interface for CredentialsApi.proposeCredential. Will send a proposal.
 */
interface ProposeCredentialOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions {
  connectionId: string;
  protocolVersion: CredentialProtocolVersionType<CPs>;
  credentialFormats: DidCommCredentialFormatPayload<CredentialFormatsFromProtocols<CPs>, 'createProposal'>;
}
/**
 * Interface for CredentialsApi.acceptProposal. Will send an offer
 *
 * credentialFormats is optional because this is an accept method
 */
interface AcceptCredentialProposalOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions {
  credentialExchangeRecordId: string;
  credentialFormats?: DidCommCredentialFormatPayload<CredentialFormatsFromProtocols<CPs>, 'acceptProposal'>;
}
/**
 * Interface for CredentialsApi.negotiateProposal. Will send an offer
 */
interface NegotiateCredentialProposalOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions {
  credentialExchangeRecordId: string;
  credentialFormats: DidCommCredentialFormatPayload<CredentialFormatsFromProtocols<CPs>, 'createOffer'>;
}
/**
 * Interface for CredentialsApi.createOffer. Will create an out of band offer
 */
interface CreateCredentialOfferOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions {
  protocolVersion: CredentialProtocolVersionType<CPs>;
  credentialFormats: DidCommCredentialFormatPayload<CredentialFormatsFromProtocols<CPs>, 'createOffer'>;
}
/**
 * Interface for CredentialsApi.offerCredential. Extends CreateCredentialOfferOptions, will send an offer
 */
interface OfferCredentialOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions, CreateCredentialOfferOptions<CPs> {
  connectionId: string;
}
/**
 * Interface for CredentialsApi.acceptOffer. Will send a request
 *
 * credentialFormats is optional because this is an accept method
 */
interface AcceptCredentialOfferOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions {
  credentialExchangeRecordId: string;
  credentialFormats?: DidCommCredentialFormatPayload<CredentialFormatsFromProtocols<CPs>, 'acceptOffer'>;
}
/**
 * Interface for CredentialsApi.negotiateOffer. Will send a proposal.
 */
interface NegotiateCredentialOfferOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions {
  credentialExchangeRecordId: string;
  credentialFormats: DidCommCredentialFormatPayload<CredentialFormatsFromProtocols<CPs>, 'createProposal'>;
}
/**
 * Interface for CredentialsApi.acceptRequest. Will send a credential
 *
 * credentialFormats is optional because this is an accept method
 */
interface AcceptCredentialRequestOptions<CPs extends DidCommCredentialProtocol[] = DidCommCredentialProtocol[]> extends BaseOptions {
  credentialExchangeRecordId: string;
  credentialFormats?: DidCommCredentialFormatPayload<CredentialFormatsFromProtocols<CPs>, 'acceptRequest'>;
  autoAcceptCredential?: DidCommAutoAcceptCredential;
  comment?: string;
}
/**
 * Interface for CredentialsApi.acceptCredential. Will send an ack message
 */
interface AcceptCredentialOptions {
  credentialExchangeRecordId: string;
}
/**
 * Interface for CredentialsApi.sendRevocationNotification. Will send a revoke message
 */
interface SendRevocationNotificationOptions {
  connectionId?: string;
  credentialExchangeRecordId?: string;
  revocationId: string;
  revocationFormat: string;
  comment?: string;
  requestAck?: boolean;
}
/**
 * Interface for CredentialsApi.sendProblemReport. Will send a problem-report message
 */
interface SendCredentialProblemReportOptions {
  credentialExchangeRecordId: string;
  description: string;
}
/**
 * Interface for CredentialsApi.declineOffer. Decline a received credential offer and optionally send a problem-report message to Issuer.
 */
interface DeclineCredentialOfferOptions {
  credentialExchangeRecordId: string;
  /**
   * Whether to send a problem-report message to the issuer as part
   * of declining the credential offer
   */
  sendProblemReport?: boolean;
  /**
   * Description to include in the problem-report message
   * Only used if `sendProblemReport` is set to `true`.
   * @default "Offer declined"
   */
  problemReportDescription?: string;
}
//#endregion
export { AcceptCredentialOfferOptions, AcceptCredentialOptions, AcceptCredentialProposalOptions, AcceptCredentialRequestOptions, CreateCredentialOfferOptions, CredentialProtocolVersionType, DeclineCredentialOfferOptions, FindCredentialMessageReturn, FindCredentialOfferMessageReturn, FindCredentialProposalMessageReturn, FindCredentialRequestMessageReturn, NegotiateCredentialOfferOptions, NegotiateCredentialProposalOptions, OfferCredentialOptions, ProposeCredentialOptions, SendCredentialProblemReportOptions, SendRevocationNotificationOptions };
//# sourceMappingURL=DidCommCredentialsApiOptions.d.mts.map