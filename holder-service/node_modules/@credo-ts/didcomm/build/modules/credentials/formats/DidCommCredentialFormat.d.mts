//#region src/modules/credentials/formats/DidCommCredentialFormat.d.ts
/**
 * Get the payload for a specific method from a list of CredentialFormat interfaces and a method
 *
 * @example
 * ```
 *
 * type CreateOfferCredentialFormats = CredentialFormatPayload<[IndyCredentialFormat, JsonLdCredentialFormat], 'createOffer'>
 *
 * // equal to
 * type CreateOfferCredentialFormats = {
 *  indy: {
 *   // ... params for indy create offer ...
 *  },
 *  jsonld: {
 *  // ... params for jsonld create offer ...
 *  }
 * }
 * ```
 */
type DidCommCredentialFormatPayload<CFs extends DidCommCredentialFormat[], M extends keyof DidCommCredentialFormat['credentialFormats']> = { [CredentialFormat in CFs[number] as CredentialFormat['formatKey']]?: CredentialFormat['credentialFormats'][M] };
interface DidCommCredentialFormat {
  formatKey: string;
  credentialRecordType: string;
  credentialFormats: {
    createProposal: unknown;
    acceptProposal: unknown;
    createOffer: unknown;
    acceptOffer: unknown;
    createRequest: unknown;
    acceptRequest: unknown;
  };
  formatData: {
    proposal: unknown;
    offer: unknown;
    request: unknown;
    credential: unknown;
  };
}
//#endregion
export { DidCommCredentialFormat, DidCommCredentialFormatPayload };
//# sourceMappingURL=DidCommCredentialFormat.d.mts.map