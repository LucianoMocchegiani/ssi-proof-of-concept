import { DidCommMessage } from "../../../DidCommMessage.mjs";
import { DidCommProblemReportMessage } from "../../../messages/problem-reports/DidCommProblemReportMessage.mjs";
import "../../../messages/index.mjs";
import { DidCommCredentialState } from "../models/DidCommCredentialState.mjs";
import { DidCommCredentialRole } from "../models/DidCommCredentialRole.mjs";
import { DidCommCredentialExchangeRecord } from "../repository/DidCommCredentialExchangeRecord.mjs";
import { ExtractCredentialFormats } from "../formats/DidCommCredentialFormatServiceOptions.mjs";
import { DidCommCredentialFormatService } from "../formats/DidCommCredentialFormatService.mjs";
import "../formats/index.mjs";
import { DidCommMessageHandlerRegistry } from "../../../DidCommMessageHandlerRegistry.mjs";
import "../repository/index.mjs";
import { AcceptCredentialOfferOptions, AcceptCredentialOptions, AcceptCredentialProposalOptions, AcceptCredentialRequestOptions, CreateCredentialOfferOptions, CreateCredentialProblemReportOptions, CreateCredentialProposalOptions, CreateCredentialRequestOptions, CredentialProtocolMsgReturnType, DeleteCredentialOptions, GetCredentialFormatDataReturn, NegotiateCredentialOfferOptions, NegotiateCredentialProposalOptions } from "./DidCommCredentialProtocolOptions.mjs";
import { DidCommCredentialProtocol } from "./DidCommCredentialProtocol.mjs";
import { DidCommInboundMessageContext } from "../../../models/DidCommInboundMessageContext.mjs";
import "../../../models/index.mjs";
import { DidCommFeatureRegistry } from "../../../DidCommFeatureRegistry.mjs";
import { AgentContext, Query, QueryOptions } from "@credo-ts/core";

//#region src/modules/credentials/protocol/DidCommBaseCredentialProtocol.d.ts
/**
 * Base implementation of the DidCommCredentialProtocol that can be used as a foundation for implementing
 * the DidCommCredentialProtocol interface.
 */
declare abstract class DidCommBaseCredentialProtocol<CFs extends DidCommCredentialFormatService[] = DidCommCredentialFormatService[]> implements DidCommCredentialProtocol<CFs> {
  abstract readonly version: string;
  protected abstract getFormatServiceForRecordType(credentialRecordType: string): CFs[number];
  abstract createProposal(agentContext: AgentContext, options: CreateCredentialProposalOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract processProposal(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  abstract acceptProposal(agentContext: AgentContext, options: AcceptCredentialProposalOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract negotiateProposal(agentContext: AgentContext, options: NegotiateCredentialProposalOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract createOffer(agentContext: AgentContext, options: CreateCredentialOfferOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract processOffer(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  abstract acceptOffer(agentContext: AgentContext, options: AcceptCredentialOfferOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract negotiateOffer(agentContext: AgentContext, options: NegotiateCredentialOfferOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract createRequest(agentContext: AgentContext, options: CreateCredentialRequestOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract processRequest(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  abstract acceptRequest(agentContext: AgentContext, options: AcceptCredentialRequestOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract processCredential(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  abstract acceptCredential(agentContext: AgentContext, options: AcceptCredentialOptions): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  abstract processAck(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  abstract createProblemReport(agentContext: AgentContext, options: CreateCredentialProblemReportOptions): Promise<CredentialProtocolMsgReturnType<DidCommProblemReportMessage>>;
  abstract findProposalMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  abstract findOfferMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  abstract findRequestMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  abstract findCredentialMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  abstract getFormatData(agentContext: AgentContext, credentialExchangeId: string): Promise<GetCredentialFormatDataReturn<ExtractCredentialFormats<CFs>>>;
  abstract register(messageHandlerRegistry: DidCommMessageHandlerRegistry, featureRegistry: DidCommFeatureRegistry): void;
  /**
   * Process a received credential {@link DidCommProblemReportMessage}.
   *
   * @param messageContext The message context containing a credential problem report message
   * @returns credential record associated with the credential problem report message
   */
  processProblemReport(messageContext: DidCommInboundMessageContext<DidCommProblemReportMessage>): Promise<DidCommCredentialExchangeRecord>;
  /**
   * Update the record to a new state and emit an state changed event. Also updates the record
   * in storage.
   *
   * @param credentialExchangeRecord The credential record to update the state for
   * @param newState The state to update to
   *
   */
  updateState(agentContext: AgentContext, credentialExchangeRecord: DidCommCredentialExchangeRecord, newState: DidCommCredentialState): Promise<void>;
  protected emitStateChangedEvent(agentContext: AgentContext, credentialExchangeRecord: DidCommCredentialExchangeRecord, previousState: DidCommCredentialState | null): void;
  /**
   * Retrieve a credential record by id
   *
   * @param credentialRecordId The credential record id
   * @throws {RecordNotFoundError} If no record is found
   * @return The credential record
   *
   */
  getById(agentContext: AgentContext, credentialRecordId: string): Promise<DidCommCredentialExchangeRecord>;
  /**
   * Retrieve all credential records
   *
   * @returns List containing all credential records
   */
  getAll(agentContext: AgentContext): Promise<DidCommCredentialExchangeRecord[]>;
  findAllByQuery(agentContext: AgentContext, query: Query<DidCommCredentialExchangeRecord>, queryOptions?: QueryOptions): Promise<DidCommCredentialExchangeRecord[]>;
  /**
   * Find a credential record by id
   *
   * @param credentialRecordId the credential record id
   * @returns The credential record or null if not found
   */
  findById(agentContext: AgentContext, proofExchangeRecordId: string): Promise<DidCommCredentialExchangeRecord | null>;
  delete(agentContext: AgentContext, credentialExchangeRecord: DidCommCredentialExchangeRecord, options?: DeleteCredentialOptions): Promise<void>;
  /**
   * Retrieve a credential record by connection id and thread id
   *
   * @param properties Properties to query by
   *
   * @throws {RecordNotFoundError} If no record is found
   * @throws {RecordDuplicateError} If multiple records are found
   * @returns The credential record
   */
  getByProperties(agentContext: AgentContext, properties: {
    threadId: string;
    role?: DidCommCredentialRole;
    connectionId?: string;
  }): Promise<DidCommCredentialExchangeRecord>;
  /**
   * Find a credential record by connection id and thread id, returns null if not found
   *
   * @param threadId The thread id
   * @param role The role of the record, i.e. holder or issuer
   * @param connectionId The connection id
   *
   * @returns The credential record
   */
  findByProperties(agentContext: AgentContext, properties: {
    threadId: string;
    role?: DidCommCredentialRole;
    connectionId?: string;
  }): Promise<DidCommCredentialExchangeRecord | null>;
  update(agentContext: AgentContext, credentialExchangeRecord: DidCommCredentialExchangeRecord): Promise<void>;
}
//#endregion
export { DidCommBaseCredentialProtocol };
//# sourceMappingURL=DidCommBaseCredentialProtocol.d.mts.map