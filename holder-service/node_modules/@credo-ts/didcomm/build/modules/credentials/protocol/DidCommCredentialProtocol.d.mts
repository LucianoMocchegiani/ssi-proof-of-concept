import { DidCommMessage } from "../../../DidCommMessage.mjs";
import { DidCommProblemReportMessage } from "../../../messages/problem-reports/DidCommProblemReportMessage.mjs";
import "../../../messages/index.mjs";
import { DidCommCredentialState } from "../models/DidCommCredentialState.mjs";
import { DidCommCredentialRole } from "../models/DidCommCredentialRole.mjs";
import { DidCommCredentialExchangeRecord } from "../repository/DidCommCredentialExchangeRecord.mjs";
import { ExtractCredentialFormats } from "../formats/DidCommCredentialFormatServiceOptions.mjs";
import { DidCommCredentialFormatService } from "../formats/DidCommCredentialFormatService.mjs";
import "../formats/index.mjs";
import { DidCommMessageHandlerRegistry } from "../../../DidCommMessageHandlerRegistry.mjs";
import "../repository/index.mjs";
import { AcceptCredentialOfferOptions, AcceptCredentialOptions, AcceptCredentialProposalOptions, AcceptCredentialRequestOptions, CreateCredentialOfferOptions, CreateCredentialProblemReportOptions, CreateCredentialProposalOptions, CreateCredentialRequestOptions, CredentialProtocolMsgReturnType, DeleteCredentialOptions, GetCredentialFormatDataReturn, NegotiateCredentialOfferOptions, NegotiateCredentialProposalOptions } from "./DidCommCredentialProtocolOptions.mjs";
import { DidCommInboundMessageContext } from "../../../models/DidCommInboundMessageContext.mjs";
import "../../../models/index.mjs";
import { DidCommFeatureRegistry } from "../../../DidCommFeatureRegistry.mjs";
import { AgentContext, Query, QueryOptions } from "@credo-ts/core";

//#region src/modules/credentials/protocol/DidCommCredentialProtocol.d.ts
interface DidCommCredentialProtocol<CFs extends DidCommCredentialFormatService[] = DidCommCredentialFormatService[]> {
  readonly version: string;
  createProposal(agentContext: AgentContext, options: CreateCredentialProposalOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  processProposal(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  acceptProposal(agentContext: AgentContext, options: AcceptCredentialProposalOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  negotiateProposal(agentContext: AgentContext, options: NegotiateCredentialProposalOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  createOffer(agentContext: AgentContext, options: CreateCredentialOfferOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  processOffer(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  acceptOffer(agentContext: AgentContext, options: AcceptCredentialOfferOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  negotiateOffer(agentContext: AgentContext, options: NegotiateCredentialOfferOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  createRequest(agentContext: AgentContext, options: CreateCredentialRequestOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  processRequest(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  acceptRequest(agentContext: AgentContext, options: AcceptCredentialRequestOptions<CFs>): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  processCredential(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  acceptCredential(agentContext: AgentContext, options: AcceptCredentialOptions): Promise<CredentialProtocolMsgReturnType<DidCommMessage>>;
  processAck(messageContext: DidCommInboundMessageContext<DidCommMessage>): Promise<DidCommCredentialExchangeRecord>;
  createProblemReport(agentContext: AgentContext, options: CreateCredentialProblemReportOptions): Promise<CredentialProtocolMsgReturnType<DidCommProblemReportMessage>>;
  processProblemReport(messageContext: DidCommInboundMessageContext<DidCommProblemReportMessage>): Promise<DidCommCredentialExchangeRecord>;
  findProposalMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  findOfferMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  findRequestMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  findCredentialMessage(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommMessage | null>;
  getFormatData(agentContext: AgentContext, credentialExchangeId: string): Promise<GetCredentialFormatDataReturn<ExtractCredentialFormats<CFs>>>;
  updateState(agentContext: AgentContext, credentialExchangeRecord: DidCommCredentialExchangeRecord, newState: DidCommCredentialState): Promise<void>;
  getById(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommCredentialExchangeRecord>;
  getAll(agentContext: AgentContext): Promise<DidCommCredentialExchangeRecord[]>;
  findAllByQuery(agentContext: AgentContext, query: Query<DidCommCredentialExchangeRecord>, queryOptions?: QueryOptions): Promise<DidCommCredentialExchangeRecord[]>;
  findById(agentContext: AgentContext, credentialExchangeId: string): Promise<DidCommCredentialExchangeRecord | null>;
  delete(agentContext: AgentContext, credentialExchangeRecord: DidCommCredentialExchangeRecord, options?: DeleteCredentialOptions): Promise<void>;
  getByProperties(agentContext: AgentContext, properties: {
    threadId: string;
    connectionId?: string;
    role?: DidCommCredentialRole;
  }): Promise<DidCommCredentialExchangeRecord>;
  findByProperties(agentContext: AgentContext, properties: {
    threadId: string;
    connectionId?: string;
    role?: DidCommCredentialRole;
  }): Promise<DidCommCredentialExchangeRecord | null>;
  update(agentContext: AgentContext, credentialExchangeRecord: DidCommCredentialExchangeRecord): Promise<void>;
  register(messageHandlerRegistry: DidCommMessageHandlerRegistry, featureRegistry: DidCommFeatureRegistry): void;
}
//#endregion
export { DidCommCredentialProtocol };
//# sourceMappingURL=DidCommCredentialProtocol.d.mts.map