import { ParsedMessageType } from "../../../../../util/messageType.mjs";
import { CredentialPreviewOptions, DidCommCredentialPreviewAttribute } from "../../../models/DidCommCredentialPreviewAttribute.mjs";
import "../../../../../index.mjs";

//#region src/modules/credentials/protocol/v2/messages/DidCommCredentialV2Preview.d.ts
/**
 * Credential preview inner message class.
 *
 * This is not a message but an inner object for other messages in this protocol. It is used construct a preview of the data for the credential.
 *
 * @see https://github.com/hyperledger/aries-rfcs/tree/main/features/0453-issue-credential-v2#preview-credential
 */
declare class DidCommCredentialV2Preview {
  constructor(options: CredentialPreviewOptions);
  static readonly type: ParsedMessageType;
  readonly type: string;
  attributes: DidCommCredentialPreviewAttribute[];
  toJSON(): Record<string, unknown>;
  /**
   * Create a credential preview from a record with name and value entries.
   *
   * @example
   * const preview = CredentialPreview.fromRecord({
   *   name: "Bob",
   *   age: "20"
   * })
   */
  static fromRecord(record: Record<string, string>): DidCommCredentialV2Preview;
}
//#endregion
export { DidCommCredentialV2Preview };
//# sourceMappingURL=DidCommCredentialV2Preview.d.mts.map