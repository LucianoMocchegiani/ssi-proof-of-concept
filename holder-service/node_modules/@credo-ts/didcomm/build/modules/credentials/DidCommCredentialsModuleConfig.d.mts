import { DidCommAutoAcceptCredential } from "./models/DidCommCredentialAutoAcceptType.mjs";
import { DidCommCredentialProtocol } from "./protocol/DidCommCredentialProtocol.mjs";

//#region src/modules/credentials/DidCommCredentialsModuleConfig.d.ts
/**
 * CredentialsModuleConfigOptions defines the interface for the options of the CredentialsModuleConfig class.
 * This can contain optional parameters that have default values in the config class itself.
 */
interface DidCommCredentialsModuleConfigOptions<CredentialProtocols extends DidCommCredentialProtocol[]> {
  /**
   * Whether to automatically accept credential messages. Applies to all issue credential protocol versions.
   *
   * @default {@link DidCommAutoAcceptCredential.Never}
   */
  autoAcceptCredentials?: DidCommAutoAcceptCredential;
  /**
   * Credential protocols to make available to the credentials module. Only one credential protocol should be registered for each credential
   * protocol version.
   *
   * When not provided, the `DidCommCredentialV2Protocol` is registered by default.
   *
   * @default
   * ```
   * [DidCommCredentialV2Protocol]
   * ```
   */
  credentialProtocols: CredentialProtocols;
}
declare class DidCommCredentialsModuleConfig<CredentialProtocols extends DidCommCredentialProtocol[]> {
  private options;
  constructor(options: DidCommCredentialsModuleConfigOptions<CredentialProtocols>);
  /** See {@link DidCommCredentialsModuleConfigOptions.autoAcceptCredentials} */
  get autoAcceptCredentials(): DidCommAutoAcceptCredential;
  /** See {@link DidCommCredentialsModuleConfigOptions.credentialProtocols} */
  get credentialProtocols(): CredentialProtocols;
}
//#endregion
export { DidCommCredentialsModuleConfig, DidCommCredentialsModuleConfigOptions };
//# sourceMappingURL=DidCommCredentialsModuleConfig.d.mts.map