import { DidCommDidExchangeRole } from "../models/DidCommDidExchangeRole.mjs";
import "../models/index.mjs";
import { DidCommConnectionRecord } from "./DidCommConnectionRecord.mjs";
import { AgentContext, EventEmitter, Repository, StorageService } from "@credo-ts/core";

//#region src/modules/connections/repository/DidCommConnectionRepository.d.ts
declare class DidCommConnectionRepository extends Repository<DidCommConnectionRecord> {
  constructor(storageService: StorageService<DidCommConnectionRecord>, eventEmitter: EventEmitter);
  findByDids(agentContext: AgentContext, {
    ourDid,
    theirDid
  }: {
    ourDid: string;
    theirDid: string;
  }): Promise<DidCommConnectionRecord | null>;
  getByThreadId(agentContext: AgentContext, threadId: string): Promise<DidCommConnectionRecord>;
  getByRoleAndThreadId(agentContext: AgentContext, role: DidCommDidExchangeRole, threadId: string): Promise<DidCommConnectionRecord>;
}
//#endregion
export { DidCommConnectionRepository };
//# sourceMappingURL=DidCommConnectionRepository.d.mts.map