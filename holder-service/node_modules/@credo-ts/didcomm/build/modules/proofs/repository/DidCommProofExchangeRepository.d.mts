import { DidCommProofExchangeRecord } from "./DidCommProofExchangeRecord.mjs";
import { AgentContext, EventEmitter, Repository, StorageService } from "@credo-ts/core";

//#region src/modules/proofs/repository/DidCommProofExchangeRepository.d.ts
declare class DidCommProofExchangeRepository extends Repository<DidCommProofExchangeRecord> {
  constructor(storageService: StorageService<DidCommProofExchangeRecord>, eventEmitter: EventEmitter);
  /**
   * Retrieve a proof record by connection id and thread id
   *
   * @param connectionId The connection id
   * @param threadId The thread id
   * @throws {RecordNotFoundError} If no record is found
   * @throws {RecordDuplicateError} If multiple records are found
   * @returns The proof record
   */
  getByThreadAndConnectionId(agentContext: AgentContext, threadId: string, connectionId?: string): Promise<DidCommProofExchangeRecord>;
  /**
   * Retrieve proof records by connection id and parent thread id
   *
   * @param connectionId The connection id
   * @param parentThreadId The parent thread id
   * @returns List containing all proof records matching the given query
   */
  getByParentThreadAndConnectionId(agentContext: AgentContext, parentThreadId: string, connectionId?: string): Promise<DidCommProofExchangeRecord[]>;
}
//#endregion
export { DidCommProofExchangeRepository };
//# sourceMappingURL=DidCommProofExchangeRepository.d.mts.map