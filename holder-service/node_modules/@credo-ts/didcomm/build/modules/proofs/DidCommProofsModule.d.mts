import { DidCommProofProtocol } from "./protocol/DidCommProofProtocol.mjs";
import { DidCommProofsModuleConfig, DidCommProofsModuleConfigOptions } from "./DidCommProofsModuleConfig.mjs";
import { DidCommProofsApi } from "./DidCommProofsApi.mjs";
import { DidCommProofV2Protocol } from "./protocol/v2/DidCommProofV2Protocol.mjs";
import "./protocol/index.mjs";
import { AgentContext, ApiModule, Constructor, DependencyManager, Optional } from "@credo-ts/core";

//#region src/modules/proofs/DidCommProofsModule.d.ts
/**
 * Default proofProtocols that will be registered if the `proofProtocols` property is not configured.
 */
type DefaultDidCommProofProtocols = [DidCommProofV2Protocol<[]>];
type DidCommProofsModuleOptions<ProofProtocols extends DidCommProofProtocol[]> = Optional<DidCommProofsModuleConfigOptions<ProofProtocols>, 'proofProtocols'>;
declare class DidCommProofsModule<ProofProtocols extends DidCommProofProtocol[] = DefaultDidCommProofProtocols> implements ApiModule {
  readonly config: DidCommProofsModuleConfig<ProofProtocols>;
  readonly api: Constructor<DidCommProofsApi<ProofProtocols>>;
  constructor(config?: DidCommProofsModuleOptions<ProofProtocols>);
  /**
   * Registers the dependencies of the proofs module on the dependency manager.
   */
  register(dependencyManager: DependencyManager): void;
  initialize(agentContext: AgentContext): Promise<void>;
}
//#endregion
export { DefaultDidCommProofProtocols, DidCommProofsModule, DidCommProofsModuleOptions };
//# sourceMappingURL=DidCommProofsModule.d.mts.map