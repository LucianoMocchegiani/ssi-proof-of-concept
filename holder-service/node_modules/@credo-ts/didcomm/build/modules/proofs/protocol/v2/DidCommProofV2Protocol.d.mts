import { DidCommMessage } from "../../../../DidCommMessage.mjs";
import { DidCommProblemReportMessage } from "../../../../messages/problem-reports/DidCommProblemReportMessage.mjs";
import "../../../../messages/index.mjs";
import { DidCommMessageHandlerRegistry } from "../../../../DidCommMessageHandlerRegistry.mjs";
import { DidCommProofExchangeRecord } from "../../repository/DidCommProofExchangeRecord.mjs";
import "../../repository/index.mjs";
import { DidCommProofFormatService } from "../../formats/DidCommProofFormatService.mjs";
import { AcceptPresentationOptions, AcceptProofProposalOptions, AcceptProofRequestOptions, CreateProofProblemReportOptions, CreateProofProposalOptions, CreateProofRequestOptions, GetCredentialsForRequestOptions, GetCredentialsForRequestReturn, GetProofFormatDataReturn, NegotiateProofProposalOptions, NegotiateProofRequestOptions, ProofProtocolMsgReturnType, SelectCredentialsForRequestOptions, SelectCredentialsForRequestReturn } from "../DidCommProofProtocolOptions.mjs";
import { DidCommProofProtocol } from "../DidCommProofProtocol.mjs";
import { DidCommBaseProofProtocol } from "../DidCommBaseProofProtocol.mjs";
import { DidCommPresentationV2AckMessage } from "./messages/DidCommPresentationV2AckMessage.mjs";
import { DidCommPresentationV2Message } from "./messages/DidCommPresentationV2Message.mjs";
import { DidCommProposePresentationV2Message } from "./messages/DidCommProposePresentationV2Message.mjs";
import { DidCommRequestPresentationV2Message } from "./messages/DidCommRequestPresentationV2Message.mjs";
import "./messages/index.mjs";
import { DidCommInboundMessageContext } from "../../../../models/DidCommInboundMessageContext.mjs";
import "../../../../models/index.mjs";
import { DidCommFeatureRegistry } from "../../../../DidCommFeatureRegistry.mjs";
import { AgentContext } from "@credo-ts/core";

//#region src/modules/proofs/protocol/v2/DidCommProofV2Protocol.d.ts
interface DidCommProofV2ProtocolConfig<ProofFormatServices extends DidCommProofFormatService[]> {
  proofFormats: ProofFormatServices;
}
declare class DidCommProofV2Protocol<PFs extends DidCommProofFormatService[] = DidCommProofFormatService[]> extends DidCommBaseProofProtocol implements DidCommProofProtocol<PFs> {
  private proofFormatCoordinator;
  private proofFormats;
  constructor({
    proofFormats
  }: DidCommProofV2ProtocolConfig<PFs>);
  /**
   * The version of the present proof protocol this service supports
   */
  readonly version: "v2";
  register(messageHandlerRegistry: DidCommMessageHandlerRegistry, featureRegistry: DidCommFeatureRegistry): void;
  createProposal(agentContext: AgentContext, {
    connectionRecord,
    proofFormats,
    comment,
    autoAcceptProof,
    goalCode,
    goal,
    parentThreadId
  }: CreateProofProposalOptions<PFs>): Promise<{
    proofRecord: DidCommProofExchangeRecord;
    message: DidCommMessage;
  }>;
  /**
   * Method called by {@link V2ProposeCredentialHandler} on reception of a propose presentation message
   * We do the necessary processing here to accept the proposal and do the state change, emit event etc.
   * @param messageContext the inbound propose presentation message
   * @returns proof record appropriate for this incoming message (once accepted)
   */
  processProposal(messageContext: DidCommInboundMessageContext<DidCommProposePresentationV2Message>): Promise<DidCommProofExchangeRecord>;
  acceptProposal(agentContext: AgentContext, {
    proofRecord,
    proofFormats,
    autoAcceptProof,
    comment,
    goalCode,
    goal,
    willConfirm
  }: AcceptProofProposalOptions<PFs>): Promise<ProofProtocolMsgReturnType<DidCommRequestPresentationV2Message>>;
  /**
   * Negotiate a proof proposal as verifier (by sending a proof request message) to the connection
   * associated with the proof record.
   *
   * @param options configuration for the request see {@link NegotiateProofProposalOptions}
   * @returns Proof exchange record associated with the proof request
   *
   */
  negotiateProposal(agentContext: AgentContext, {
    proofRecord,
    proofFormats,
    autoAcceptProof,
    comment,
    goalCode,
    goal,
    willConfirm
  }: NegotiateProofProposalOptions<PFs>): Promise<ProofProtocolMsgReturnType<DidCommRequestPresentationV2Message>>;
  /**
   * Create a {@link DidCommRequestPresentationV2Message} as beginning of protocol process.
   * @returns Object containing request message and associated credential record
   *
   */
  createRequest(agentContext: AgentContext, {
    proofFormats,
    autoAcceptProof,
    comment,
    connectionRecord,
    parentThreadId,
    goalCode,
    goal,
    willConfirm
  }: CreateProofRequestOptions<PFs>): Promise<ProofProtocolMsgReturnType<DidCommRequestPresentationV2Message>>;
  /**
   * Process a received {@link DidCommRequestPresentationV2Message}. This will not accept the proof request
   * or send a proof. It will only update the existing proof record with
   * the information from the proof request message. Use {@link createCredential}
   * after calling this method to create a proof.
   *z
   * @param messageContext The message context containing a v2 proof request message
   * @returns proof record associated with the proof request message
   *
   */
  processRequest(messageContext: DidCommInboundMessageContext<DidCommRequestPresentationV2Message>): Promise<DidCommProofExchangeRecord>;
  acceptRequest(agentContext: AgentContext, {
    proofRecord,
    autoAcceptProof,
    comment,
    proofFormats,
    goalCode,
    goal
  }: AcceptProofRequestOptions<PFs>): Promise<{
    proofRecord: DidCommProofExchangeRecord;
    message: DidCommPresentationV2Message;
  }>;
  /**
   * Create a {@link DidCommProposePresentationV2Message} as response to a received credential request.
   * To create a proposal not bound to an existing proof exchange, use {@link createProposal}.
   *
   * @param options configuration to use for the proposal
   * @returns Object containing proposal message and associated proof record
   *
   */
  negotiateRequest(agentContext: AgentContext, {
    proofRecord,
    proofFormats,
    autoAcceptProof,
    comment,
    goalCode,
    goal
  }: NegotiateProofRequestOptions<PFs>): Promise<ProofProtocolMsgReturnType<DidCommProposePresentationV2Message>>;
  getCredentialsForRequest(agentContext: AgentContext, {
    proofRecord,
    proofFormats
  }: GetCredentialsForRequestOptions<PFs>): Promise<GetCredentialsForRequestReturn<PFs>>;
  selectCredentialsForRequest(agentContext: AgentContext, {
    proofRecord,
    proofFormats
  }: SelectCredentialsForRequestOptions<PFs>): Promise<SelectCredentialsForRequestReturn<PFs>>;
  processPresentation(messageContext: DidCommInboundMessageContext<DidCommPresentationV2Message>): Promise<DidCommProofExchangeRecord>;
  acceptPresentation(agentContext: AgentContext, {
    proofRecord
  }: AcceptPresentationOptions): Promise<ProofProtocolMsgReturnType<DidCommPresentationV2AckMessage>>;
  processAck(messageContext: DidCommInboundMessageContext<DidCommPresentationV2AckMessage>): Promise<DidCommProofExchangeRecord>;
  createProblemReport(_agentContext: AgentContext, {
    description,
    proofRecord
  }: CreateProofProblemReportOptions): Promise<ProofProtocolMsgReturnType<DidCommProblemReportMessage>>;
  shouldAutoRespondToProposal(agentContext: AgentContext, options: {
    proofRecord: DidCommProofExchangeRecord;
    proposalMessage: DidCommProposePresentationV2Message;
  }): Promise<boolean>;
  shouldAutoRespondToRequest(agentContext: AgentContext, options: {
    proofRecord: DidCommProofExchangeRecord;
    requestMessage: DidCommRequestPresentationV2Message;
  }): Promise<boolean>;
  shouldAutoRespondToPresentation(agentContext: AgentContext, options: {
    proofRecord: DidCommProofExchangeRecord;
    presentationMessage: DidCommPresentationV2Message;
  }): Promise<boolean>;
  findRequestMessage(agentContext: AgentContext, proofExchangeRecordId: string): Promise<DidCommRequestPresentationV2Message | null>;
  findPresentationMessage(agentContext: AgentContext, proofExchangeRecordId: string): Promise<DidCommPresentationV2Message | null>;
  findProposalMessage(agentContext: AgentContext, proofExchangeRecordId: string): Promise<DidCommProposePresentationV2Message | null>;
  getFormatData(agentContext: AgentContext, proofExchangeRecordId: string): Promise<GetProofFormatDataReturn>;
  /**
   * Get all the format service objects for a given proof format from an incoming message
   * @param messageFormats the format objects containing the format name (eg indy)
   * @return the proof format service objects in an array - derived from format object keys
   */
  private getFormatServicesFromMessage;
  /**
   * Get all the format service objects for a given proof format
   * @param proofFormats the format object containing various optional parameters
   * @return the proof format service objects in an array - derived from format object keys
   */
  private getFormatServices;
  private getFormatServiceForFormatKey;
  private getFormatServiceForFormat;
}
//#endregion
export { DidCommProofV2Protocol, DidCommProofV2ProtocolConfig };
//# sourceMappingURL=DidCommProofV2Protocol.d.mts.map