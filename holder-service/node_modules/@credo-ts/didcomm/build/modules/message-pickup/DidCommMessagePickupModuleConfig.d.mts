import { DidCommMessagePickupProtocol } from "./protocol/DidCommMessagePickupProtocol.mjs";

//#region src/modules/message-pickup/DidCommMessagePickupModuleConfig.d.ts
/**
 * DidCommMessagePickupModuleConfigOptions defines the interface for the options of the DidCommMessagePickupModuleConfig class.
 * This can contain optional parameters that have default values in the config class itself.
 */
interface DidCommMessagePickupModuleConfigOptions<DidCommMessagePickupProtocols extends DidCommMessagePickupProtocol[]> {
  /**
   * Maximum number of messages to retrieve in a single batch message pickup
   *
   * @default 10
   */
  maximumBatchSize?: number;
  /**
   * Message pickup protocols to make available to the message pickup module. Only one protocol should be registered for each
   * protocol version.
   *
   * When not provided, DidCommMessagePickupV1Protocol and DidCommMessagePickupV2Protocol` are registered by default.
   *
   * @default
   * ```
   * [DidCommMessagePickupV1Protocol, DidCommMessagePickupV2Protocol]
   * ```
   */
  protocols: DidCommMessagePickupProtocols;
}
declare class DidCommMessagePickupModuleConfig<MessagePickupProtocols extends DidCommMessagePickupProtocol[]> {
  private options;
  constructor(options: DidCommMessagePickupModuleConfigOptions<MessagePickupProtocols>);
  /** See {@link DidCommMessagePickupModuleConfig.maximumBatchSize} */
  get maximumBatchSize(): number;
  /** See {@link DidCommMessagePickupModuleConfig.protocols} */
  get protocols(): MessagePickupProtocols;
}
//#endregion
export { DidCommMessagePickupModuleConfig, DidCommMessagePickupModuleConfigOptions };
//# sourceMappingURL=DidCommMessagePickupModuleConfig.d.mts.map