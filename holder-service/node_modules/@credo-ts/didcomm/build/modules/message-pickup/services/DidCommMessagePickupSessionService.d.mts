import { DidCommMessagePickupSession, DidCommMessagePickupSessionRole } from "../DidCommMessagePickupSession.mjs";
import { AgentContext } from "@credo-ts/core";

//#region src/modules/message-pickup/services/DidCommMessagePickupSessionService.d.ts
/**
 * @internal
 * The Message Pickup session service keeps track of all {@link DidCommMessagePickupSession}
 *
 * It is initially intended for Message Holder/Mediator role, where only Live Mode sessions are
 * considered.
 */
declare class DidCommMessagePickupSessionService {
  private sessions;
  constructor();
  start(agentContext: AgentContext): void;
  getLiveSession(_agentContext: AgentContext, sessionId: string): DidCommMessagePickupSession | undefined;
  getLiveSessionByConnectionId(_agentContext: AgentContext, options: {
    connectionId: string;
    role?: DidCommMessagePickupSessionRole;
  }): DidCommMessagePickupSession | undefined;
  saveLiveSession(agentContext: AgentContext, options: {
    connectionId: string;
    protocolVersion: string;
    role: DidCommMessagePickupSessionRole;
    transportSessionId: string;
  }): void;
  removeLiveSession(agentContext: AgentContext, options: {
    connectionId: string;
  }): void;
}
//#endregion
export { DidCommMessagePickupSessionService };
//# sourceMappingURL=DidCommMessagePickupSessionService.d.mts.map