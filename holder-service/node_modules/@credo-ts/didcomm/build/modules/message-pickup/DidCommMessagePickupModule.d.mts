import { DidCommMessagePickupProtocol } from "./protocol/DidCommMessagePickupProtocol.mjs";
import { DidCommMessagePickupModuleConfig, DidCommMessagePickupModuleConfigOptions } from "./DidCommMessagePickupModuleConfig.mjs";
import { DidCommMessagePickupV1Protocol } from "./protocol/v1/DidCommMessagePickupV1Protocol.mjs";
import { DidCommMessagePickupV2Protocol } from "./protocol/v2/DidCommMessagePickupV2Protocol.mjs";
import "./protocol/index.mjs";
import { DidCommMessagePickupApi } from "./DidCommMessagePickupApi.mjs";
import { AgentContext, ApiModule, Constructor, DependencyManager, Optional } from "@credo-ts/core";

//#region src/modules/message-pickup/DidCommMessagePickupModule.d.ts
/**
 * Default protocols that will be registered if the `protocols` property is not configured.
 */
type DefaultDidCommMessagePickupProtocols = [DidCommMessagePickupV1Protocol, DidCommMessagePickupV2Protocol];
type DidCommMessagePickupModuleOptions<MessagePickupProtocols extends DidCommMessagePickupProtocol[]> = Optional<DidCommMessagePickupModuleConfigOptions<MessagePickupProtocols>, 'protocols'>;
declare class DidCommMessagePickupModule<MessagePickupProtocols extends DidCommMessagePickupProtocol[] = DefaultDidCommMessagePickupProtocols> implements ApiModule {
  readonly config: DidCommMessagePickupModuleConfig<MessagePickupProtocols>;
  readonly api: Constructor<DidCommMessagePickupApi<MessagePickupProtocols>>;
  constructor(config?: DidCommMessagePickupModuleOptions<MessagePickupProtocols>);
  /**
   * Registers the dependencies of the message pickup answer module on the dependency manager.
   */
  register(dependencyManager: DependencyManager): void;
  initialize(agentContext: AgentContext): Promise<void>;
  onInitializeContext(agentContext: AgentContext): Promise<void>;
}
//#endregion
export { DefaultDidCommMessagePickupProtocols, DidCommMessagePickupModule, DidCommMessagePickupModuleOptions };
//# sourceMappingURL=DidCommMessagePickupModule.d.mts.map