import { ParsedMessageType } from "../../../util/messageType.mjs";
import { DidCommMessage } from "../../../DidCommMessage.mjs";
import "../../../index.mjs";

//#region src/modules/routing/messages/DidCommMediationRequestMessage.d.ts
interface DidCommMediationRequestMessageOptions {
  sentTime?: Date;
  id?: string;
  locale?: string;
}
/**
 * This message serves as a request from the recipient to the mediator, asking for the permission (and routing information)
 * to publish the endpoint as a mediator.
 *
 * @see https://github.com/hyperledger/aries-rfcs/blob/master/features/0211-route-coordination/README.md#mediation-request
 */
declare class DidCommMediationRequestMessage extends DidCommMessage {
  /**
   * Create new DidCommBasicMessage instance.
   * sentTime will be assigned to new Date if not passed, id will be assigned to uuid/v4 if not passed
   * @param options
   */
  constructor(options: DidCommMediationRequestMessageOptions);
  readonly type: string;
  static readonly type: ParsedMessageType;
}
//#endregion
export { DidCommMediationRequestMessage, DidCommMediationRequestMessageOptions };
//# sourceMappingURL=DidCommMediationRequestMessage.d.mts.map