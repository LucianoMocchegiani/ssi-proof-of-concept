import { __decorate } from "../../../_virtual/_@oxc-project_runtime@0.110.0/helpers/decorate.mjs";
import { IsValidMessageType, parseMessageType } from "../../../util/messageType.mjs";
import { __decorateMetadata } from "../../../_virtual/_@oxc-project_runtime@0.110.0/helpers/decorateMetadata.mjs";
import { DidCommMessage } from "../../../DidCommMessage.mjs";

//#region src/modules/routing/messages/DidCommMediationRequestMessage.ts
/**
* This message serves as a request from the recipient to the mediator, asking for the permission (and routing information)
* to publish the endpoint as a mediator.
*
* @see https://github.com/hyperledger/aries-rfcs/blob/master/features/0211-route-coordination/README.md#mediation-request
*/
var DidCommMediationRequestMessage = class DidCommMediationRequestMessage extends DidCommMessage {
	/**
	* Create new DidCommBasicMessage instance.
	* sentTime will be assigned to new Date if not passed, id will be assigned to uuid/v4 if not passed
	* @param options
	*/
	constructor(options) {
		super();
		this.type = DidCommMediationRequestMessage.type.messageTypeUri;
		if (options) {
			this.id = options.id || this.generateId();
			this.addLocale(options.locale || "en");
		}
	}
};
DidCommMediationRequestMessage.type = parseMessageType("https://didcomm.org/coordinate-mediation/1.0/mediate-request");
__decorate([IsValidMessageType(DidCommMediationRequestMessage.type), __decorateMetadata("design:type", Object)], DidCommMediationRequestMessage.prototype, "type", void 0);

//#endregion
export { DidCommMediationRequestMessage };
//# sourceMappingURL=DidCommMediationRequestMessage.mjs.map