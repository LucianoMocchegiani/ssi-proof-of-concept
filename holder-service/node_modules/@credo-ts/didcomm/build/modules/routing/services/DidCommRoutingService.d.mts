import { DidCommMediationRecipientService } from "./DidCommMediationRecipientService.mjs";
import { DidCommRouting } from "../../../models/DidCommRouting.mjs";
import "../../../models/index.mjs";
import { AgentContext, EventEmitter, Kms } from "@credo-ts/core";

//#region src/modules/routing/services/DidCommRoutingService.d.ts
declare class DidCommRoutingService {
  private mediationRecipientService;
  private eventEmitter;
  constructor(mediationRecipientService: DidCommMediationRecipientService, eventEmitter: EventEmitter);
  getRouting(agentContext: AgentContext, {
    mediatorId,
    useDefaultMediator
  }?: GetRoutingOptions): Promise<DidCommRouting>;
  removeRouting(agentContext: AgentContext, options: RemoveRoutingOptions): Promise<void>;
}
interface GetRoutingOptions {
  /**
   * Identifier of the mediator to use when setting up routing
   */
  mediatorId?: string;
  /**
   * Whether to use the default mediator if available and `mediatorId` has not been provided
   * @default true
   */
  useDefaultMediator?: boolean;
}
interface RemoveRoutingOptions {
  /**
   * Keys to remove routing from
   */
  recipientKeys: Kms.PublicJwk<Kms.Ed25519PublicJwk>[];
  /**
   * Identifier of the mediator used when routing has been set up
   */
  mediatorId: string;
}
//#endregion
export { DidCommRoutingService, GetRoutingOptions, RemoveRoutingOptions };
//# sourceMappingURL=DidCommRoutingService.d.mts.map