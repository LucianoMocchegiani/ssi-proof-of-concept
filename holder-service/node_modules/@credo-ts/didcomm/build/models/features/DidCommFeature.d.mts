//#region src/models/features/DidCommFeature.d.ts
interface DidCommFeatureOptions {
  id: string;
  type: string;
}
declare class DidCommFeature {
  id: string;
  constructor(props: DidCommFeatureOptions);
  readonly type: string;
  /**
   * Combine this feature with another one, provided both are from the same type
   * and have the same id
   *
   * @param feature object to combine with this one
   * @returns a new object resulting from the combination between this and feature
   */
  combine(feature: this): DidCommFeature;
  toJSON(): Record<string, unknown>;
}
//#endregion
export { DidCommFeature, DidCommFeatureOptions };
//# sourceMappingURL=DidCommFeature.d.mts.map